<p class="text-success"><%= notice %></p>

<%= render @document %>

<div class="mt-4">
  <div class="d-flex">
    <%= link_to "Edit this document", edit_document_path(@document), class: "btn btn-info me-2" %>
    <%= link_to "Back", dependents_path, class: "btn btn-dark me-2" %>
    <%= button_to "Destroy this document", @document, method: :delete, class: "btn btn-danger" %>
  </div>
</div>
